package com.rawprogramming.games.tests;

import static org.junit.Assert.assertTrue;

import org.junit.Before;
import org.junit.Test;

import com.rawprogramming.games.towers.AreaAttack;
import com.rawprogramming.games.towers.Explosion;
import com.rawprogramming.games.towers.Projectile;
import com.rawprogramming.games.towers.ProjectileAttack;

public class TowerAttackTest {
  
  ProjectileAttack projAttack;
  AreaAttack areaAttack;
  Projectile proj;
  Explosion expl;

  @Before
  public void setUp() throws Exception {
/*    GameApp app = new GameApp();
    app.create();
    projAttack = new ProjectileAttack(1, 2, 3);();
    areaAttack = new AreaAttack(1, 2, 3);
    proj = new Projectile(position, enemy, damage, 2, 200);
    expl = new Explosion(position, damage);*/
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }

  @Test
  public void testFindTargetsInRange() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }
  
  @Test
  public void testFindTargets() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }
  
  @Test
  public void testAttackTargets() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }
  
  @Test
  public void testGetCopy() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }
  
  @Test
  public void testExplode() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }
  
  @Test
  public void testMoveToTargets() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }
  
  @Test
  public void testApplyDamage() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }
  
  @Test
  public void testDestroyDud() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }
}
