package com.rawprogramming.games.tests;

import static org.junit.Assert.assertTrue;

import org.junit.Before;
import org.junit.Test;

import com.rawprogramming.games.towers.Tower;
import com.rawprogramming.games.towers.TowerStore;

public class TowerTest {

  TowerStore store;
  Tower tower;
  
  @Before
  public void setUp() throws Exception {
/*    GameApp app = new GameApp();
    app.create();
    store = TowerStore.getInstance();
    tower = new Tower("BasicTower", 100, new ProjectileAttack(5, 1.5f, 1f));*/
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }

  @Test
  public void testBuyTower() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }

  @Test
  public void testGenerateTowers() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }

  @Test
  public void testAddMoney() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }

  @Test
  public void testAttackTargets() {
    assertTrue("Cannot implement due to libGDX dependecies", true);
  }
}
